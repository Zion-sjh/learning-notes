##为什么预编译能防止 SQL 注入？

没有预编译时（字符串拼接）：
如果你拼了一句 WHERE name = ' + name + '，而用户输入的是 ' OR '1'='1。数据库拿到的完整 SQL 是：WHERE name = '' OR '1'='1'。
这改变了 SQL 的逻辑结构，黑客绕过了验证。

有了预编译后（参数绑定）：
数据库已经认定：WHERE name = ? 里的问号处只能是一个单纯的字符串参数。即便用户输入 ' OR '1'='1，数据库也会乖乖地把它当成一个完整的姓名去匹配。
此时，数据库找的是那个“名叫 ' OR '1'='1 ”的人，而不会把 OR 当成逻辑运算符。


## 为什么要用连接池？ 
1. 性能提升：减少开销
创建一个数据库连接是非常“贵”的。它涉及：

    建立 TCP 连接（三次握手）。
    身份验证（发送用户名、密码）。
    数据库分配资源（内存、上下文）。
    执行完后的销毁（四次挥手）。

连接池在启动时就预先创建好了一批连接，业务请求来时直接复用，省去了反复创建和销毁的时间。
  
2. 资源控制：防止数据库崩溃

如果没有连接池，高并发下（比如双 11 开抢）瞬间涌入 10000 个请求，程序就会尝试建立 10000 个连接。

    后果：数据库的内存和 CPU 会被撑爆，直接宕机。

    连接池的作用：它像一个限流阀门。你可以设置最大连接数为 100。当第 101 个请求进来时，它必须在池外排队等待。这样虽然请求慢一点，但保证了数据库不会崩。

3. 连接有效性管理
连接久了可能会因为网络抖动或数据库端主动断开而失效。连接池（如 Druid、HikariCP）会自动进行“心跳检测”，发现死连接会踢出去并补个新的，保证你拿到的连接永远是活着的。
  
4. 统一配置与监控
通过连接池，你可以统一管理超时时间、最大空闲时间等参数。现代连接池（尤其是 HikariCP，目前 Spring Boot 默认的池）还能提供监控指标，让你一眼看出系统现在的负载情况。



































































































  




  
