在 Java 中：
子类构造函数执行之前，一定会先执行父类构造函数
而且：
默认调用的是父类的无参构造（super()）

new Child()
  ↓
调用 Child()
  ↓
先调用 super()
  ↓
执行 Parent()
  ↓
再执行 Child()


如果父类没有无参构造函数
class Child extends Parent {
    public Child() {
        // 编译报错！
    }
}
除非
class Child extends Parent {
    public Child() {
        super("Tom");
    }
}


super 和 this 不能同时出现
原因：
this()：调用本类构造
super()：调用父类构造

👉 它们都必须是第一行

👉 所以只能存在一个


##char类型在Java中是一个16位的Unicode字符类型,表示范围是从0到65535(十六进制为0x0000到0xFFFF)


##抽象类可以实现（implements）接口，接口不能继承（extends）抽象类
抽象类可以拥有构造方法，接口不能拥有构造方法
接口中的方法默认都是public abstract的，而抽象类中的方法可以有不同的访问修饰符，包括public、protected和默认访问级别（default）



















